# Infrastructure — sappkeys

<!-- UPDATE WHEN: hosting changes -->

None. Desktop instrument + CLI; builds and runs locally. Repo on GitHub
(`localteampartners/sappkeys`). No VPS, no monitor, no secrets.
